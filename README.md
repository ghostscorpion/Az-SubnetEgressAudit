# Az-SubnetEgressAudit
Scans all accessible Azure subscriptions and evaluates each subnet to determine whether explicit outbound connectivity is configured or if the subnet may be relying on legacy default outbound access.
